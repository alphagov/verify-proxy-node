apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Release.Name }}-hub-egress
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: proxy-node
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  hosts:
  - {{ .Values.hubFqdn }}
  gateways:
  - {{ .Release.Name }}-hub-egress
  - mesh
  tls:
  - match:
    - gateways:
      - {{ .Release.Name }}-hub-egress
      port: 443
      sniHosts:
      - {{ .Values.hubFqdn }}
    route:
    - destination:
        host: {{ .Values.hubFqdn }}
        port:
          number: 443
      weight: 100
  - match:
    - gateways:
      - mesh
      port: 443
      sniHosts:
      - {{ .Values.hubFqdn }}
    route:
    - destination:
        host: istio-egressgateway.istio-system.svc.cluster.local
        subset: {{ .Release.Name }}-hub-egress
        port:
          number: 443
      weight: 100
