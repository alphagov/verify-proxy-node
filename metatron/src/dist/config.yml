server:
  applicationConnectors:
    - type: http
      port: ${PORT:-6670}
  adminConnectors:
    - type: http
      port: ${ADMIN_PORT:-6671}
  requestLog:
    appenders:
      - type: console

logging:
  level: ${LOG_LEVEL:-INFO}
  appenders:
    - type: ${LOGGING_APPENDER:-logstash-console}
