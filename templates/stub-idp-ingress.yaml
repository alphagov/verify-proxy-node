apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-stub-idp
  labels:
    app.kubernetes.io/name: stub-idp
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - hosts:
      - {{ .Release.Name }}-stub-idp.{{ .Values.cluster.domain }}
      secretName: {{ .Release.Name }}-stub-idp
  rules:
    - host: {{ .Release.Name }}-stub-idp.{{ .Values.cluster.domain }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Release.Name }}-stub-idp
              servicePort: 6200
